[project]
name = "numia"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110,<1",
    "uvicorn[standard]>=0.27,<1",
    "pydantic>=2.6,<3",
]

[tool.vercel]
entrypoint = "backend.app.main:app"
